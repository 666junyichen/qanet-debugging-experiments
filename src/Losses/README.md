# Losses

Question-answering loss functions for start/end span prediction.

The loss module supports negative-log-likelihood style training and cross-entropy variants used while validating output/loss consistency during debugging.

