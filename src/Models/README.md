# Models

QANet model components for extractive question answering.

Includes:

- Context-query attention and multi-head attention components.
- Convolutional, embedding, encoder, and output-head modules.
- Dropout, activation, initialization, and normalization variants used in debugging and experiments.

